import styled from 'styled-components';

export const Container = styled.div`
  width: 90%;
  max-width: 1080px;
  margin: 2rem auto 0;

  h2 {
    font-size: 1.5rem;
    margin-bottom: .25rem;
    color: var(--grey);
  }
`;
